# Gatway底层架构

## 网络层

Gatway通常基于Netty等高性能网络框架实现，这可以让他高效的处理大量请求。

Netty提供了事件驱动的一步网络框架，非常适合实现高性能网关。

## 请求分发器

核心组件，负责接收HTTP请求并将其分发到对应的处理逻辑。

通常包含一组过滤器（Filters）和路由（ROuter）的调用练

## 路由匹配器

根据请求URL、方法、头信息等匹配预定义的路由规则

## 过滤器链

过滤器可以对请求进行处理、比如 身份验证、日志记录、请求修改等。

。